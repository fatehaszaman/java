# Java

LeetCode problems solved in Java. Three problems so far, picked specifically because the Java type system makes them more interesting than the Python equivalents.

## Problems

| File | Problem |
|---|---|
| `CoinChange332.java` | LC #332 -- Coin Change (DP, bottom-up) |
| `KthLargestElementInAnArray215.java` | LC #215 -- Kth Largest (QuickSelect / min-heap) |
| `PowXN50.java` | LC #50 -- Pow(x, n) (fast exponentiation) |

## Stack

Java
